{
  "name": "internshala-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently -c \"bgBlue.bold,bgGreen.bold\" \"npm:start-backend\" \"npm:start-frontend\"",
    "start-backend": "cd backend && npm start",
    "start-frontend": "cd internarea && npm run dev", 
    "build": "npm run build --prefix internarea"


  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
